export default class Project {
    constructor(title, date, desc, hashtags, links, imgUrl) {
      this.title = title;
      this.date = date;
      this.desc = desc;
      this.hashtags = hashtags;
      this.links = links;
      this.imgUrl = imgUrl;
    }
}